server:
  port: 8089

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:postgresql://localhost:5433/jobfinder_notification_db
    username: yedi
    password: yedi1235
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    # ✅ ADD CONSUMER CONFIG HERE
    consumer:
      group-id: notification-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.value.default.type: com.example.notificationservice.dto.NotificationEvent
        spring.json.use.type.headers: false
        spring.json.trusted.packages: "*"

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:yediworku@gmail.com}
    password: ${MAIL_PASSWORD:gpzzuaiwogssbtxe}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true