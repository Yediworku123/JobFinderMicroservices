server:
  port: 8087

spring:
  application:
    name: job-service

  # ⬇️ Moved datasource UNDER spring
  datasource:
    url: jdbc:postgresql://localhost:5433/jobfinder_jobs_db
    username: yedi
    password: yedi1235
    driver-class-name: org.postgresql.Driver

  # ⬇️ Moved jpa UNDER spring
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # ⬇️ Moved security UNDER spring
#  security:
   # oauth2:
      #resourceserver:
      #  jwt:
          #issuer-uri: http://localhost:8280/realms/jobfindermultiserver

# ⬇️ Moved kafka to the ROOT level (it has its own prefix)
kafka:
  bootstrap-servers: localhost:9092
  producer:
    key-serializer: org.apache.kafka.common.serialization.StringSerializer
    value-serializer: org.springframework.kafka.support.serializer.JsonSerializer